%h1 Listing reviews

%table
  %tr
    %th Trunk branch
    %th Development branch
    %th Closed
    %th Submitter
    %th Project
    %th
    %th
    %th

  - @reviews.each do |review|
    %tr
      %td= review.submitter.name
      %td= review.project.name
      %td= review.trunk_branch
      %td= review.development_branch
      %td= review.close_date
      %td= link_to 'Show', review
      %td= link_to 'Edit', edit_review_path(review)
      %td= link_to 'Destroy', review, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Review', new_review_path
